# Frontend

Carpeta reservada para el frontend React + Vite definido

Debe consumir el API Gateway en `http://localhost:8080` y enviar JWT en rutas protegidas mediante:

```http
Authorization: Bearer <token>
```

Ver detalles en `../GUIA_SERVICIOS_EQUIPO.md`.
